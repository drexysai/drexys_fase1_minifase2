<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cadastro Médico - Dr. Exys</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Inter', sans-serif;
      background-color: #f8feff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Header */
    .header {
        background: #FFF;
        padding: 1rem 2rem;
        box-shadow: 0 1px 0 rgba(34, 84, 116, 0.1);
    }

    .logo-container {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-family: 'Outfit', sans-serif;
            font-size: 1.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #225474 0%, #29A8AF 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .logo-icon {
            font-size: 2rem;
        }

        /* Back button */
        .back-container {
            text-align: center;
            padding: 2rem;
            max-width: 600px;
            margin: 0 auto;
            width: 100%;
        }

        .back-button {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            color: #225474;
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            font-family: 'Inter', sans-serif;
            transition: opacity 0.2s;
        }
        
        .back-button:hover {
            opacity: 0.8;
        }

    .main-container {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .register-card {
      background: #fff;
      border-radius: 16px;
      padding: 4rem;
      width: 100%;
      max-width: 680px;
      box-shadow: 0 8px 32px rgba(34, 84, 116, 0.12);
      border: 1px solid rgba(34, 84, 116, 0.08);
    }

    .logo {
      text-align: center;
      margin-bottom: 2rem;
    }

    .logo h1 {
      font-size: 2.5rem;
      font-weight: 600;
      font-family: 'Outfit', sans-serif;
      color: #29A8AF;
      
    }

    .logo p {
      font-size: 1.1rem;
      color: #225474;
    }

    .medical-badge {
      background: linear-gradient(120deg, #f8feff 0%, rgba(41, 168, 175, 0.1) 100%);
      border: 1px solid rgba(34, 84, 116, 0.2);
      color: #225474;
      padding: 1rem 1.5rem;
      border-radius: 10px;
      margin: 1.5rem 0 2rem;
      text-align: center;
      font-size: 1rem;
      font-weight: 500;
    }

    .form-group {
      margin-bottom: 2rem;
    }

    .form-input::placeholder {
            color: #94a3b8;
        }


    .form-row {
  display: flex;
  gap: 1rem;
  margin-bottom: 2rem;
}

.form-row .half-width {
  flex: 1;
}


    label {
      display: block;
      margin-bottom: 0.75rem;
      color: #225474;
      font-size: 1rem;
      font-weight: 600;
    }

    input {
      width: 100%;
      padding: 1.25rem 1.5rem;
      border: 1px solid rgba(34, 84, 116, 0.2);
      border-radius: 10px;
      font-size: 1.1rem;
    }

    input:focus {
      outline: none;
      border-color: #29A8AF;
      box-shadow: 0 0 0 3px rgba(41, 168, 175, 0.1);
    }

    .submit-btn {
      width: 100%;
      padding: 1.25rem;
      background: linear-gradient(135deg, #225474 0%, #29A8AF 100%);
      color: #fff;
      border: none;
      border-radius: 10px;
      font-size: 1.2rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
      box-shadow: 0 4px 16px rgba(34, 84, 116, 0.25);
    }

    .submit-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 4px 16px rgba(34, 84, 116, 0.3);
    }

    .link {
      text-align: center;
      margin-top: 2rem;
      font-size: 1rem;
    }

    .link a {
      color: #29A8AF;
      text-decoration: none;
      font-weight: 600;
    }

    .link a:hover {
      text-decoration: underline;
    }

    /* Terms */
        .terms {    
            text-align: center;
            font-size: 0.875rem;
            color: #64748b;
            line-height: 1.6;
            font-family: 'Inter', sans-serif;
            margin-bottom: -2rem;
        }
        
        .terms a {
            color: #29A8AF;
            text-decoration: underline;
        }

    

    .message {
      padding: 1rem;
      border-radius: 10px;
      margin-bottom: 1rem;
      text-align: center;
      font-weight: 500;
    }

    .message.success {
      background: rgba(209, 250, 229, 0.9);
      color: #065f46;
      border: 1px solid rgba(5, 150, 105, 0.3);
    }

    .message.error {
      background: rgba(254, 226, 226, 0.9);
      color: #991b1b;
      border: 1px solid rgba(239, 68, 68, 0.3);
    }
  </style>
</head>
<body>

<!-- Header -->
    <header class="header">
        <div class="logo-container">
            <span class="logo-icon">🏥</span>
            <span>DR. EXYS</span>
        </div>
    </header>

  <main class="main-container">
    <div class="register-card">
      <div class="logo">
        <h1>Registrar</h1>
      </div>

      <div class="medical-badge">
        🩺 Registro exclusivo para profissionais de saúde
      </div>

      <div id="message"></div>

      <form id="registerForm">
        <div class="form-group">
          <label for="email">Email</label>
          <input class="form-input" type="email" id="email" name="email" required placeholder="dr.joao@clinica.com" />
        </div>
        <!--
        <div class="form-group">
          <label for="username">Nome de Usuário</label>
          <input class="form-input" type="text" id="username" name="username" required placeholder="dr_joao_silva" />
        </div>
        -->

        <div class="form-row">
  <div class="form-group half-width">
    <label for="password">Senha</label>
    <input class="form-input" type="password" id="password" name="password" required placeholder="Sua senha segura" />
  </div>
  <div class="form-group half-width">
    <label for="password_confirm">Confirmar Senha</label>
    <input class="form-input" type="password" id="password_confirm" name="password_confirm" required placeholder="Confirme sua senha" />
  </div>
</div>

        <button type="submit" class="submit-btn" id="submitBtn">Criar Conta</button>
      </form>

      <div class="link">
        Já possui conta? <a href="/auth/login">Faça login aqui</a>
      </div>

      <!-- Back button  -->
    <div class="back-container">
        <a href="/" class="back-button">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M19 12H5M12 19l-7-7 7-7"/>
            </svg>
            Voltar
        </a>
    </div>

    <div class="terms">
                Ao continuar, você concorda com nossos <a href="/termos-uso">Termos de Uso</a> e confirma que leu nossa <a href="/politica-privacidade">Política de Privacidade</a>
            </div>

     
  </main>

  <script type="module">
    const form = document.getElementById('registerForm');
    const messageDiv = document.getElementById('message');
    const submitBtn = document.getElementById('submitBtn');

    const authService = {
      async register(data) {
        const response = await fetch('http://192.168.1.4:8081/api/v1/auth/register/', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data)
        });

        if (!response.ok) {
          const error = await response.json();
          throw error;
        }

        return await response.json();
      }
    };

    function showMessage(msg, type = 'error') {
      messageDiv.innerHTML = `<div class="message ${type}">${msg}</div>`;
      setTimeout(() => { messageDiv.innerHTML = ''; }, 6000);
    }

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const formData = new FormData(form);
      const data = {
        email: formData.get('email'),
        username: formData.get('username'),
        password: formData.get('password'),
        password_confirm: formData.get('password_confirm')
      };

      if (data.password !== data.password_confirm) {
        showMessage('As senhas não coincidem.');
        return;
      }

      try {
        submitBtn.disabled = true;
        submitBtn.textContent = 'Criando conta...';
        await authService.register(data);
        showMessage('Conta criada com sucesso! Redirecionando...', 'success');
        setTimeout(() => {
          window.location.href = '/auth/login';
        }, 2000);
      } catch (error) {
        let msg = 'Erro ao criar conta.';
        if (error.message) msg = error.message;
        showMessage(msg);
      } finally {
        submitBtn.disabled = false;
        submitBtn.textContent = 'Criar Conta Médica';
      }
    });
  </script>
</body>
</html>
